export type Option = {
  label: string;
  value: string;
};

export type CategoryIcon = {
  icon: string;
  link: string;
  isMore?: boolean;
};

export type HeroData = {
  title: string;
  subtitle: string;
};
